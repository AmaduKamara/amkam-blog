<div class="container mb-3 shadow py-2 px-5 border-top rounded sticky-top bg-white d-flex justify-content-between align-items-center">
   <h1><%= link_to "Amkam Blog", root_path, class:"nav-link text-info" %></h1>
   <div>
      <% if user_signed_in? %>
         Signed in as <%= current_user.email %>. This cannot be cheese?
         <%= link_to "Sign out", destroy_user_session_path, :method => :delete, class: "border rounded py-2 px-4 border-info text-decoration-none text-info mx-3" %>
      <% else %>
         <%= link_to 'Register', new_user_registration_path, class: "border rounded py-2 px-4 border-info text-decoration-none text-info mx-3" %> <%= link_to 'Sign in', new_user_session_path, class: "border rounded py-2 px-4 bg-info text-white text-decoration-none" %>
      <% end %>
   </div>
</div>