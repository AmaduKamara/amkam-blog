<%= render 'users/u_card' %>

<div class="d-flex justify-content-center">
  <div>
    <% if current_user == @user %>
      <div class="mb-4 d-flex justify-content-center">
        <a><%= link_to "Add new post", '/posts/new', class:"btn border-info border-2 text-info" %></a>
      </div>
    <% end %>
    <%= render 'posts/post' %>
  </div>
</div>